import React from 'react'

const Select = ({
    lable,
    options,
    className,
    ...props
}) => {
  return (
    <div className={`w-full `}>
        <select name="" id=""></select>
    </div>
  )
}

export default Select